# Cohere API Configuration
# Get your API key from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your_cohere_api_key_here

# Qdrant Vector Database Configuration
# For Qdrant Cloud: https://cloud.qdrant.io/
QDRANT_URL=https://your-qdrant-instance-url
QDRANT_API_KEY=your_qdrant_api_key_here

# LLM API Configuration
# Option 1: Use OpenRouter (recommended for free models)
# Get your API key from: https://openrouter.ai/keys
# The application automatically detects OPENROUTER_API_KEY and uses https://openrouter.ai/api/v1
# Note: Free models like meta-llama/llama-3.2-3b-instruct:free do not support tool calling,
# so retrieval is done manually and context is passed directly to the model.
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# Option 2: Use OpenAI (requires paid account)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Optional: Override default model
# For OpenRouter: meta-llama/llama-3.2-3b-instruct:free (default), or other models from openrouter.ai/models
# For OpenAI: gpt-4o-mini, gpt-4-turbo-preview, gpt-3.5-turbo, etc.
AGENT_MODEL=meta-llama/llama-3.2-3b-instruct:free

# Optional: Override default collection name
# COLLECTION_NAME=web_documents

# Optional: Override chunking configuration
# CHUNK_SIZE=512
# CHUNK_OVERLAP=50

# Optional: Override embedding configuration
# EMBEDDING_MODEL=embed-english-v3.0
# EMBEDDING_DIM=1024

# Optional: Override batch processing configuration
# BATCH_SIZE=5
# MAX_RETRIES=3

# CORS Configuration for FastAPI Web Service
# Allow all origins for development (default)
ALLOWED_ORIGINS=*
# For production, specify allowed domains separated by commas:
# ALLOWED_ORIGINS=https://your-frontend.com,https://www.your-frontend.com

# Frontend Configuration
# Deploy_Vercel_URL = https://book-hackathon-blond.vercel.app #sitemap.xml
